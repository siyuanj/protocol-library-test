# 任务与进度说明（LabRecord 协议库）

> 更新时间：2026-08-21 · 面向：我自己梳理 + 汇报给师姐/mentor
> 一句话：**为 LabRecord 建一套「通用的、跨学科的」协议数据结构 + 管理系统地基**；先做地基（格式标准 + 规范 Schema + 渲染器 + 库/版本设计），落地实现和 MCP 解析押后。

---

## 一、师姐交给我的任务是什么

### 1. 来源与背景
- 任务来自 Google Doc **「To Do 0822 — Protocol Library」**。
- 和师姐交流后确认的**方向纠偏**（这几条是**师姐/我沟通后定的**，不是我一个人判断）：
  - 目标是**搭建数据结构 + 管理系统**，即 LabRecord 的「后台」地基；**不是**去收集数量很全的 protocol（具体 protocol 列表师姐之后会给）。
  - 必须是**通用的、跨学科的**结构（不为某个学科写死字段）。
  - 之前做的是「**关于协议的元数据目录**」（标题/来源/许可/聚类）——方向不对；现在要做的是「**协议内容本身**」的标准（材料+用量、设备+参数、逐步 QC、控制项）。
  - **我现在要做的是 Section 1 + 2**（"1 2 是我要做的"），它们是 Section 3/4 的地基。
  - **MCP / 解析（Section 5）押后**。

### 2. Google Doc 的五个部分（原始任务拆解）

| # | 部分 | 内容 | 归属 | 优先级 |
|---|---|---|---|---|
| 1 | **Protocol Format Standard** | 文献调研 → 选/改/设计一个格式 → 一段理由+局限 → 用 qPCR/WB/细胞培养三个例子重写 | 地基 | **现在做（我的活）** |
| 2 | **Canonical Schema** | 把格式固化成 JSON Schema → 三个 JSON 实例 → JSON→协议 渲染器 | 地基 | **现在做（我的活）** |
| 3 | Library / Ownership / Version | Public/Private/Lab 三库归属与权限、引用/fork/版本/血缘、提交—审核—批准、归档 | **后台系统设计** | 已给出设计稿 |
| 4 | Library Implementation | 落地到 DB + 内部 API + 网站：保存前校验、旧数据迁移、三视图、按版本建 run | **后台系统本体** | 押后（待做） |
| 5 | Parser API | LLM 把 PDF/DOCX/图片/文本 → 第 2 部分的 JSON | MCP 类 | **押后** |

### 3. 过程中师姐/我追加的具体要求
- 用 `test_protocol/` 里**前两个 PDF**（Gel Filtration、Protein Concentration）作为**真实测试数据**生成协议。
- protocol 的**前端呈现方式做几版方案**，给我和 mentor 挑。
- 参考 GitHub 上类似的优秀开源项目。
- 「今天 5am」**定时再复查一次** Todo，看还有什么要做/可优化的。

---

## 二、目前做到什么样子

### 0. 总览

| 部分 | 状态 | 说明 |
|---|---|---|
| Section 1 格式标准 | ✅ 完成 | 文献综述 + 选型 + 理由/局限 + 三个示例 |
| Section 2 规范 Schema + 渲染器 | ✅ 完成 | JSON Schema + 5 个 JSON 实例 + 渲染器 + 校验器 + demo |
| Section 3 库/权限/版本设计 | ✅ 设计稿 | 实体、三库权限、fork/版本/血缘、审核流程、归档 |
| Section 4 落地实现 | ⏳ 待做 | DB + API + 网站（下一步大头） |
| Section 5 解析 / MCP | ⏸ 押后 | 按优先级搁置 |
| 前端多版方案 | ✅ 完成 | 4 版界面（Document / Checklist / Timeline / Compact） |
| 真实 PDF 测试 | ✅ 完成 | 前两个 PDF 已生成进标准格式 |
| 5am 定时复查 | ⚠️ **脚本就绪，尚未注册** | 一条命令即可安装，见下方「待确认」 |

### 1. 交付物清单（都在 `protocol-standard/`）

| 文件 | 是什么 | 对应 Doc |
|---|---|---|
| `01-literature-summary.md` | 协议报告/表示标准综述（MIQE、Nature Protocols/STAR/protocols.io、Bioschemas、SMART/EXACT、ISA、FAIR…）+ 对比表 + 引用 | 1.1 |
| `02-format-standard.md` | 选定的格式 + **理由段落** + 局限 | 1.2/1.3 |
| `canonical-schema.json` | 固定 JSON Schema（draft 2020-12，全局 `additionalProperties:false`） | 2.1/2.2 |
| `examples/{western-blot,qpcr,cell-culture}.{md,json}` | 三个必做示例（WB 来自你真实 PDF；qPCR 对齐 MIQE；细胞培养标准传代） | 1.4 / 2.3 |
| `examples/{gel-filtration,protein-concentration}.{md,json}` | **两个真实 PDF 生成的测试** | 追加要求 |
| `renderer/renderer.js` + `ProtocolRenderer.jsx` | JSON→协议 渲染器（原生 JS 版 + React 版） | 2.4 |
| `renderer/validate.mjs` | 校验器（结构 + 引用完整性，0 依赖） | 支撑 2/4 |
| `renderer/demo.html` | 自包含 demo：5 个协议实时渲染 + 看 JSON 源 + 看 Schema | 2.4 展示 |
| `renderer/design-options.html` | **前端四版方案对比页** | 追加要求 |
| `03-library-version-design.md` | Section 3 管理系统设计（实体/权限矩阵/版本血缘/审核/归档） | 3 |
| `presentation-plan.md` | 给 mentor 的**完整展示方案**（讲法/顺序/通用性论证/GitHub 借鉴） | 汇报 |
| `references-github-issues.md` | GitHub 真实 issue 经验对照（LabOP/ISA/Opentrons/ORD/CWL…）→ v0.2 backlog | 参考 |
| `recheck/` | 5am 本地定时复查的脚本（README + 3 个脚本） | 追加要求 |

### 2. 已验证的事实（本轮实跑，带出处）
- `node protocol-standard/renderer/validate.mjs` → **5/5 PASS**（cell-culture 10 步、gel-filtration 12 步、protein-concentration 12 步、qpcr 12 步、western-blot 18 步）。
- `canonical-schema.json` 解析通过；`parameter.type` 枚举已扩到覆盖物理/化学/生物/计算多学科（`time/temperature/volume/speed/concentration/mass/ratio/pH/count/pressure/wavelength/voltage/current/force/energy/dataSize/other …`）。
- `renderer/demo.html`（86 KB）浏览器实测：5 个协议正常渲染、含三相 Protein Concentration、**0 console 报错**。
- `renderer/design-options.html`（78 KB）：HTTP 200，四版（Document/Checklist/Timeline/Compact）齐全。

### 3. 怎么跑（现场演示）
```bash
node protocol-standard/renderer/validate.mjs          # 校验全部协议 JSON
node protocol-standard/serve.mjs                       # 起本地服务
#   → http://localhost:4173/renderer/demo.html            主渲染 demo
#   → http://localhost:4173/renderer/design-options.html  四版前端方案对比
```
或直接双击 `renderer/demo.html` / `renderer/design-options.html`（自包含，file:// 也能开）。

### 4. 几个亮点（汇报时可强调）
- **跨学科通用**：schema 无任何学科专属字段；靠「类型化参数 + 单位自由文本 + `other` 兜底」适配任意学科；5 个示例已横跨蛋白/分子/细胞/层析/定量。
- **绝不编造**：来源没写的值一律标 `needsReview`（橙色），schema 层面禁止 agent/人乱填——为 Section 5 解析打底。
- **同一份 JSON → 人类可读文档**：渲染器实时演示，且提供 4 种界面供选。
- **真实源文件可吃**：直接从你给的 PDF 生成（含三联标准曲线表）。

---

## 三、还没做 / 待你或 mentor 拍板

| 事项 | 现状 | 需要谁定 |
|---|---|---|
| **Section 4 落地实现**（DB+API+网站） | 未开始（下一步大头） | 范围与技术栈——**你/师姐** |
| **具体 protocol 列表** | 未收到（你说之后给） | **师姐提供** |
| **前端选哪一版** | 4 版已就绪 | **你 + mentor** |
| **5am 定时复查** | ⚠️ 脚本就绪，**任务未注册**（`Get-ScheduledTask` 查无） | 是否现在安装——**你确认**（它会在 5am 自动跑 `claude` 并改 `docs/STATUS.md`） |
| Schema v0.2 优化 | 已列 backlog（`Quantity{value,unit,min/max,precision}`、材料 identity/amount/role 拆分、迁移策略、JSON-LD 导出） | 我可继续 |

**5am 一键安装命令**（你说要就装；因涉及自动跑 agent，我先没擅自装）：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\大学\暑研\7-31 protocol library\protocol-standard\recheck\install-5am-task.ps1"
```

---

## 四、文件地图（快速定位）
- 想看**任务/汇报**：本文件、`protocol-standard/presentation-plan.md`
- 想看**标准与理由**：`protocol-standard/01-literature-summary.md`、`02-format-standard.md`
- 想看**数据结构**：`protocol-standard/canonical-schema.json`
- 想看**效果**：`protocol-standard/renderer/demo.html`、`design-options.html`
- 想看**管理系统设计**：`protocol-standard/03-library-version-design.md`
- 想看**GitHub 借鉴/坑**：`protocol-standard/references-github-issues.md`
